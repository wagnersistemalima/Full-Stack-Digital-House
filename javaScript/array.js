// Array = vetor = Matriz

// arrays permite armazenar varios tipos de dados em uma unica variavel, separados por indice



// criando um array, e mostrando um dado pelo indice

const pessoa = ["Wagner", 39, 1.70, true, ["Caes", "Carros"]]; // todo array começa na posição [0]

// acessando um dado especifico dentro do array, utilizando o indice
console.log(pessoa[4]);

// verificando o comprimento do array, utilizando a função .length
console.log(pessoa.length);