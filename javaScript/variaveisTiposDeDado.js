// função nativa Number. Já vimos como declarar uma variável e atribuir-lhe o tipo de dado que queremos como um valor. 

let positivo = Number("positivo");
let negativo = Number("negativo");
let decimal = Number("decimal");

console.log(positivo);

// função parseInt

const nan =  parseInt("texto");            // retorna NaN
const correto = parseInt("123");           // retorn 123